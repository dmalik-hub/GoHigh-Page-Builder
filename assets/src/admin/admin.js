/**
 * GoHigh Page Builder — Admin JS
 * Small script for post list page.
 */
import './admin.scss';

( function( $ ) {
	'use strict';

	// Ensure dashicons load on post lists.
	$( document ).ready( function() {
		// Nothing critical here for Phase 1 —
		// "Edit with GoHigh" links are added by PHP via post_row_actions.
	} );

} )( jQuery );
